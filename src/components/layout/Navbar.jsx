// This file is no longer used in the main layout
// The original Navbar.jsx and MiniNavbar.jsx are used in HomePage
import React from 'react';

const LayoutNavbar = () => {
  // This component is kept for other pages that might need a simple navbar
  // But the main HomePage uses the original Navbar and MiniNavbar components

  return (
    <div>
      {/* Simple navbar for other pages */}
    </div>
  );
};

export default LayoutNavbar;